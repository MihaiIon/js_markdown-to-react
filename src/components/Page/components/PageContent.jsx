import React from "react";
import PropTypes from "prop-types";

// Libs
// Local
import { C_NAME } from "..";

// Core
// Components

// ------------------------------------------------------

// Component's Function
function PageContent(props) {
  return <div className={`${C_NAME}_content o-wrapper`} />;
}

PageContent.propTypes = {};

export default PageContent;
